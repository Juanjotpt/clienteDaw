

    let a =20;
    let b=10;
    let palabra="hola";
    let c=10;

    console.log(a == b);  //comparamos el valor ( nos da false ).

    console.log(a != b ); // true porque los dos valores son distintos.

    console.log(a === b);  //compara que sean lo mismo y del mismo tipo de dato.

    console.log(a === palabra); //es un string con un entero.

    console.log(c === b); // es true porque son enteros con el mismo valor.

    console.log(c <=a);   // true porque es menor tenemos <  > <= >=.